# LanLink 安装与服务管理指南

## 🎯 概述

LanLink 提供两种运行方式：

1. **前台运行** - 手动启动，按 Ctrl+C 退出
2. **系统服务** - 开机自启，后台运行

本文档介绍如何安装和管理 LanLink 服务。

---

## 📦 方式一：安装到系统 PATH

将 LanLink 添加到系统 PATH，可以在任何位置执行命令。

### Windows

**以管理员身份运行 PowerShell，然后执行：**

```powershell
.\lanlink.exe install
```

**执行过程：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  安装 LanLink (Windows)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

检查权限:
✓ 已获取管理员权限

程序信息:
  当前路径: D:\tools\lanlink.exe

安装目录:
  目标位置: C:\Program Files\LanLink
✓ 目录创建成功

复制文件:
✓ 程序文件已复制

配置环境变量:
✓ 已添加到系统 PATH

创建快捷方式:
✓ 已创建启动脚本

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 安装完成!

现在您可以在任何位置使用 'lanlink' 命令了

重启终端或运行以下命令刷新环境变量:
  $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine")

测试安装:
  lanlink version
```

**验证安装：**

```powershell
# 关闭当前终端，打开新终端
lanlink version
```

### Linux/Mac

**使用 sudo 运行：**

```bash
sudo ./lanlink install
```

**执行过程：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  安装 LanLink (Unix)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

检查权限:
✓ 已获取 root 权限

程序信息:
  当前路径: /home/user/lanlink

安装:
  目标位置: /usr/local/bin/lanlink
✓ 安装完成

系统服务:
✓ 已创建 systemd 服务

启动服务:
  sudo systemctl start lanlink
  sudo systemctl enable lanlink  # 开机自启

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 安装完成!

现在您可以在任何位置使用 'lanlink' 命令了

测试安装:
  lanlink version
```

**验证安装：**

```bash
lanlink version
```

---

## 🚀 方式二：安装为系统服务（开机自启）

将 LanLink 安装为系统服务，实现开机自动启动。

### Windows

#### 1. 安装服务

**以管理员身份运行：**

```powershell
lanlink service install
```

**输出示例：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  安装 LanLink 服务 (Windows)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

检查权限:
✓ 已获取管理员权限

创建服务:
  程序路径: C:\Program Files\LanLink\lanlink.exe
✓ 服务创建成功

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 服务安装完成！

启动服务:
  lanlink service start
  或: sc start LanLink

服务将在开机时自动启动
```

#### 2. 启动服务

```powershell
lanlink service start
```

#### 3. 查看服务状态

```powershell
lanlink service status
```

**或使用 Windows 服务管理器：**

1. 按 `Win+R`
2. 输入 `services.msc`
3. 查找 "LanLink - 局域网域名自动映射"

#### 4. 管理服务

```powershell
# 停止服务
lanlink service stop

# 查看状态
lanlink service status

# 卸载服务
lanlink service uninstall
```

### Linux

#### 1. 安装服务

**使用 sudo：**

```bash
sudo lanlink service install
```

**输出示例：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  安装 LanLink 服务 (Systemd)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

检查权限:
✓ 已获取 root 权限

创建服务文件:
  程序路径: /usr/local/bin/lanlink
✓ 服务文件创建成功
  服务文件: /etc/systemd/system/lanlink.service

配置服务:
✓ Systemd 已重载
✓ 服务已启用（开机自启）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 服务安装完成！

启动服务:
  lanlink service start
  或: sudo systemctl start lanlink

查看日志:
  sudo journalctl -u lanlink -f

服务将在开机时自动启动
```

#### 2. 启动服务

```bash
sudo lanlink service start
# 或
sudo systemctl start lanlink
```

#### 3. 查看服务状态

```bash
lanlink service status
# 或
sudo systemctl status lanlink
```

#### 4. 查看日志

```bash
# 实时查看
sudo journalctl -u lanlink -f

# 查看最近100行
sudo journalctl -u lanlink -n 100
```

#### 5. 管理服务

```bash
# 停止服务
sudo lanlink service stop

# 重启服务
sudo systemctl restart lanlink

# 查看状态
lanlink service status

# 禁用开机自启（但不卸载）
sudo systemctl disable lanlink

# 启用开机自启
sudo systemctl enable lanlink

# 卸载服务
sudo lanlink service uninstall
```

### macOS

macOS 使用与 Linux 相同的命令（基于 launchd）。

---

## 🔄 两种方式对比

| 特性     | 前台运行   | 系统服务            |
| -------- | ---------- | ------------------- |
| 启动方式 | 手动       | 开机自动            |
| 运行方式 | 前台       | 后台                |
| 查看日志 | 控制台     | 日志文件/journalctl |
| 退出方式 | Ctrl+C     | 停止服务            |
| 适用场景 | 测试、调试 | 生产环境            |

---

## 📋 完整安装流程

### 推荐流程（生产环境）

#### Windows

```powershell
# 1. 以管理员身份打开 PowerShell

# 2. 安装到系统PATH
.\lanlink.exe install

# 3. 关闭并重新打开 PowerShell

# 4. 安装为系统服务
lanlink service install

# 5. 启动服务
lanlink service start

# 6. 验证运行
lanlink status
lanlink list
```

#### Linux

```bash
# 1. 安装到系统
sudo ./lanlink install

# 2. 安装为系统服务
sudo lanlink service install

# 3. 启动服务
sudo lanlink service start

# 4. 验证运行
lanlink status
lanlink list
```

---

## 🛠️ 卸载

### 卸载系统服务

**Windows:**

```powershell
# 以管理员身份运行
lanlink service uninstall
```

**Linux:**

```bash
sudo lanlink service uninstall
```

### 从系统完全卸载

**Windows:**

```powershell
# 1. 先卸载服务
lanlink service uninstall

# 2. 卸载程序
lanlink uninstall
```

**Linux:**

```bash
# 1. 先卸载服务
sudo lanlink service uninstall

# 2. 卸载程序
sudo lanlink uninstall
```

---

## 🔍 故障排查

### 服务无法启动

**Windows:**

```powershell
# 查看服务状态
sc query LanLink

# 查看事件日志
eventvwr
# 导航到: Windows 日志 -> 应用程序
```

**Linux:**

```bash
# 查看详细状态
sudo systemctl status lanlink -l

# 查看日志
sudo journalctl -u lanlink -n 50
```

### 权限问题

**现象**: 提示"需要管理员权限"

**解决**:

- Windows: 右键 PowerShell -> 以管理员身份运行
- Linux: 在命令前加 `sudo`

### 服务已存在

**现象**: "服务已存在"错误

**解决**:

```powershell
# Windows
lanlink service uninstall
lanlink service install

# Linux
sudo lanlink service uninstall
sudo lanlink service install
```

---

## 💡 高级配置

### 修改服务配置

**Linux - 编辑服务文件:**

```bash
sudo nano /etc/systemd/system/lanlink.service
```

**示例配置:**

```ini
[Unit]
Description=LanLink - 局域网域名自动映射工具
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/lanlink start
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

# 自定义配置
WorkingDirectory=/opt/lanlink
User=lanlink
Environment="LOG_LEVEL=debug"

[Install]
WantedBy=multi-user.target
```

**应用更改:**

```bash
sudo systemctl daemon-reload
sudo systemctl restart lanlink
```

### 配置文件位置

**默认配置文件位置:**

- Windows: `C:\Program Files\LanLink\config.json`
- Linux: `/etc/lanlink/config.json` 或 程序同目录

**创建配置文件:**

```bash
# Linux
sudo mkdir -p /etc/lanlink
sudo cp config.example.json /etc/lanlink/config.json
sudo nano /etc/lanlink/config.json

# 修改服务文件，指定配置位置
ExecStart=/usr/local/bin/lanlink start --config /etc/lanlink/config.json
```

---

## 📊 监控与维护

### 监控服务状态

**Windows:**

```powershell
# 定期检查（创建计划任务）
$trigger = New-ScheduledTaskTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Hours 1)
$action = New-ScheduledTaskAction -Execute "lanlink.exe" -Argument "service status"
Register-ScheduledTask -TaskName "LanLink Monitor" -Trigger $trigger -Action $action
```

**Linux:**

```bash
# 添加到 crontab
crontab -e

# 每小时检查一次
0 * * * * /usr/local/bin/lanlink service status >> /var/log/lanlink-monitor.log 2>&1
```

### 日志轮转

**Linux - 配置 logrotate:**

```bash
sudo nano /etc/logrotate.d/lanlink
```

```
/var/log/lanlink/*.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    create 0644 root root
    postrotate
        systemctl reload lanlink > /dev/null 2>&1 || true
    endscript
}
```

---

## 🎯 快速参考

### 常用命令

```bash
# 安装
lanlink install                  # 安装到PATH
lanlink service install          # 安装为服务

# 服务管理
lanlink service start            # 启动
lanlink service stop             # 停止
lanlink service status           # 状态

# 监控
lanlink status                   # 运行状态
lanlink list                     # 节点列表
lanlink logs -f                  # 实时日志

# 卸载
lanlink service uninstall        # 卸载服务
lanlink uninstall                # 完全卸载
```

---

**安装完成后，LanLink 将在后台默默工作，自动管理您的局域网域名映射！** 🚀
